# Camera, images
opencv-python==4.13.0.92
numpy==2.4.2
pillow==12.1.0

# Text finding + reading (Apple Vision, macOS only)
pyobjc-framework-Vision==12.2.2; sys_platform == "darwin"
pyobjc-framework-Quartz==12.2.2; sys_platform == "darwin"

# Questions about the captured text: local Qwen via Ollama's HTTP API
requests==2.34.2

# Optional web lookup when the text doesn't have the answer (DuckDuckGo, no API key)
ddgs==9.16.0

# Gemini reader (assitant.py)
google-generativeai==0.8.6

# Voice commands (PyAudio needs `brew install portaudio` on macOS)
SpeechRecognition==3.14.5
PyAudio==0.2.14

# Settings from .env
python-dotenv==1.2.1
